import React from 'react';
import { Card, CardContent, Typography, Box } from '@mui/material';
import { Psychology as PsychologyIcon } from '@mui/icons-material';

interface AIInterpretationProps {
  interpretation: string;
}

export const AIInterpretation: React.FC<AIInterpretationProps> = ({ interpretation }) => {
  if (!interpretation) {
    return null;
  }

  return (
    <Card sx={{ mb: 3 }}>
      <CardContent>
        <Box sx={{ display: 'flex', alignItems: 'center', mb: 2 }}>
          <PsychologyIcon sx={{ mr: 1, color: '#2196F3' }} />
          <Typography variant="h6">
            AI Analysis
          </Typography>
        </Box>
        
        <Typography variant="body1" sx={{ lineHeight: 1.7 }}>
          {interpretation}
        </Typography>

        <Typography variant="caption" color="text.secondary" sx={{ mt: 2, display: 'block' }}>
          This analysis is generated by AI and represents a probabilistic assessment based on the provided information. 
          It should not be interpreted as a definitive medical diagnosis.
        </Typography>
      </CardContent>
    </Card>
  );
};
