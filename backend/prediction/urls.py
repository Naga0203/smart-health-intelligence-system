infrom django.urls import path
from . import views

app_name = 'prediction'

urlpatterns = [
    # Prediction endpoints will be added here
    # Example: path('predict/', views.predict_disease, name='predict_disease'),
]
